# Copy this file to .env.local and fill in your keys.
# .env.local is gitignored — never commit it.

# Google Places API key (Nearby Search + Place Details + Photos).
# Required for live restaurants. If empty, the app falls back to mock data.
EXPO_PUBLIC_GOOGLE_PLACES_KEY=
